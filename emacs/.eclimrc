# Specifies the workspace directory to use -*- conf -*-
# See $ECLIPSE_HOME/configuration/config.ini for other osgi properties.
osgi.instance.area.default=@user.home/src/workspace-develop

# increase heap size
-Xmx1024M

# increase perm gen size
-XX:PermSize=64m
-XX:MaxPermSize=128m
